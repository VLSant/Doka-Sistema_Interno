Neutral chip for categories and active filter pills; pass `onRemove` to make it dismissable.

```jsx
<Tag icon={<MapPin/>} onRemove={() => clear('cwb')}>Curitiba hub</Tag>
```

Lower-emphasis than Badge — use for user-applied filters and tags.
