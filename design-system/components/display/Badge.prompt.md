Small pill for labels, counts and statuses. For shipment states prefer the soft tones (`success`, `info`, `warning`, `danger`).

```jsx
<Badge tone="success" dot>Delivered</Badge>
<Badge tone="brand" soft={false}>New</Badge>
```

`dot` adds a leading status dot; `soft={false}` gives a solid fill for emphasis.
