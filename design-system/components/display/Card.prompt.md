The base surface for everything — wrap content groups, list rows, panels. Use `interactive` for clickable cards (adds hover lift).

```jsx
<Card padding="md" interactive>…</Card>
```

Padding `none|sm|md|lg`. Soft plum-tinted shadow that deepens on hover when interactive.
