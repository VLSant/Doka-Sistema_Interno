User/driver avatar with image or auto-initials and an optional presence dot.

```jsx
<Avatar name="Marina Alves" size="md" status="online" />
```

Sizes `xs|sm|md|lg|xl`. Falls back to plum initials chip when no `src`.
