Rounded-square checkbox for multi-select and consent. Orange when checked.

```jsx
<Checkbox checked={sel} onChange={setSel} label="Require signature" />
```

Controlled via `checked` + `onChange(next)`. Optional `label`.
