Square rounded icon action for toolbars, table rows and dense controls. Always pass `aria-label`.

```jsx
<IconButton aria-label="More" variant="ghost"><MoreIcon/></IconButton>
```

Variants: `ghost`, `soft`, `primary`, `outline`. Sizes `sm|md|lg`. Pass a Lucide `<svg>` (16–20px) as children.
