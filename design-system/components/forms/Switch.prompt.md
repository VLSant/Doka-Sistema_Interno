Toggle for on/off settings and filters. Orange track when on, spring-eased knob.

```jsx
<Switch checked={liveTracking} onChange={setLiveTracking} label="Live tracking" />
```

Sizes `sm|md`. Controlled via `checked` + `onChange(next)`.
